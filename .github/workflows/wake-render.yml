name: Wake Up Render

on:
  schedule:
    # Runs at 10:00 PM UTC (5:00 PM EST)
    - cron: '0 22 * * *'
  workflow_dispatch:  # Allows manual trigger

jobs:
  wake:
    runs-on: ubuntu-latest
    steps:
    - name: Ping Render Health Endpoint
      run: |
        echo "Waking up Render at 5:00 PM EST..."
        curl -s https://domvaenergysch10.onrender.com/health
        echo ""
        echo "✓ Wake-up ping sent. Render is starting up."
